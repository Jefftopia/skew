{
  "name": "braid-sw",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/braid-sw/src",
  "projectType": "library",
  "tags": ["scope:shared", "framework:agnostic"],
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "outputPath": "dist/libs/braid-sw",
        "main": "libs/braid-sw/src/index.ts",
        "tsConfig": "libs/braid-sw/tsconfig.build.json",
        "assets": ["libs/braid-sw/*.md"]
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "vitest run --config libs/braid-sw/vite.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
