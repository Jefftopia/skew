# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular

# Emitted by `skew-stamp` on every build — the formatter would fight the generator.
apps/*/src/generated/
