/**
 * src/app/skew-build-id.generated.ts
 *
 * GENERATED by `skew-build stamp` (see the prebuild script in package.json) —
 * do not edit, and add this path to .gitignore. Shown here so the shape of
 * what the stamp step produces is concrete.
 *
 * The same identity is mirrored into index.html as
 *   <meta name="skew-build-id" content="a053212-20260810T143012Z">
 * so ops can check which build a CDN edge is serving with plain curl:
 *   curl -s https://app.example.com/ | grep skew-build-id
 *   curl -s https://app.example.com/skew-manifest.json
 * If those two disagree on an edge, that edge is the stale region.
 */
export const SKEW_BUILD_ID = 'a053212-20260810T143012Z' as const;

export const SKEW_BUILD_INFO = {
  buildId: SKEW_BUILD_ID,
  gitSha: 'a053212',
  builtAt: '2026-08-10T14:30:12.000Z',
} as const;
